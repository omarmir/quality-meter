# Low-Latency Improvement

This page is generated by `bun run report:low-latency --write-json --write-md`.

## Update Command

```bash
bun run report:low-latency --write-json --write-md
```

## At A Glance

- Cases: `300` main + `300` hard-negative
- Selected default tradeoff: `Step 2: Task-Type Structure Checks`
- Main cross-validated MAE: `4.4`
- Hard-negative MAE: `13.2`
- Compared variants: `4`

## Top-Line Metrics

| Variant | Main CV MAE | Main CV Median | Main CV Within 10 | Main CV Within 20 | Hard-Negative MAE |
| --- | ---: | ---: | ---: | ---: | ---: |
| Baseline | 4.4 | 3.1 | 261/300 | 295/300 | 13.8 |
| Step 1: Deterministic Constraint Checks | 4.4 | 3.1 | 261/300 | 295/300 | 13.8 |
| Step 2: Task-Type Structure Checks | 4.4 | 2.8 | 262/300 | 295/300 | 13.2 |
| Step 3: Criterion Normalization | 4.4 | 2.8 | 262/300 | 295/300 | 13.2 |

## Corpus Shape

This report compares low-latency helper variants on the current handwritten corpus and its matching hard-negative set.

- `4` low-latency variants
- `300` main benchmark cases
- `300` hard-negative cases

## By Domain

This artifact stores per-variant summary metrics only. Domain-level slices are not persisted for the low-latency sweep.

## By Profile

This artifact stores per-variant summary metrics only. Profile-level slices are not persisted for the low-latency sweep.

## Largest Misses

This artifact stores per-variant summary metrics only. Case-level miss lists are not persisted for the low-latency sweep.

## How To Read This Page

- The top-line table is for side-by-side comparison of each low-latency variant on the same current corpus.
- Lower hard-negative MAE is only useful when the main cross-validated MAE stays competitive.
- The selected default tradeoff is the current best benchmark result, not a permanent narrative recommendation.
